[color]
	ui = true

[core]
	pager = "delta"

[filter "lfs"]
	clean = "git-lfs clean -- %f"
	process = "git-lfs filter-process"
	required = true
	smudge = "git-lfs smudge -- %f"

[github]
	user = "alyraffauf"

[interactive]
	diffFilter = "delta --color-only"

[user]
	email = "aly@aly.codes"
	name = "Aly Raffauf"

[credential "https://github.com"]
	helper =
	helper = !gh auth git-credential

[credential "https://gist.github.com"]
	helper =
	helper = !gh auth git-credential

[credential "https://knot1.tangled.sh"]
	helper =
	helper = !tg auth git-credential

[init]
	defaultBranch = main

[fetch]
	prune = true
	pruneTags = true

[pull]
	rebase = true

[rebase]
	autoStash = true
	autoSquash = true
	updateRefs = true

[push]
	autoSetupRemote = true
	followTags = true

[rerere]
	enabled = true
	autoupdate = true

[merge]
	conflictStyle = zdiff3

[diff]
	algorithm = histogram
	colorMoved = zebra

[status]
	showUntrackedFiles = all

[branch]
	sort = -committerdate

[tag]
	sort = version:refname

[help]
	autocorrect = prompt

[alias]
	s = status -sb
	l = log --oneline --graph --decorate
	ll = log --graph --format=format:'%C(auto)%h %s %C(dim)(%an, %ar)%Creset' --all
	amend = commit --amend --no-edit
	undo = reset --soft HEAD^
	gone = !git fetch --prune && git branch -vv | awk '/: gone]/{print $1}' | xargs -r git branch -d

[credential "https://forgejo.narwhal-snapper.ts.net"]
	username = alyraffauf

[credential]
	helper = libsecret
